# Repository cleanup notes

## Safe to delete locally anytime

| Item | Why |
|------|-----|
| **`docs/`** | Generated by `node scripts/build-gh-pages.js`. Listed in `.gitignore`. GitHub Actions rebuilds it on every Pages deploy. |

## Already removed (redundant assets)

- **`frontend/src/assets/images/optimized/* - Copy.png`** — Windows duplicate files; not referenced anywhere in HTML/JS.

## Optional next cleanups (manual)

1. **Planning / internal docs** — Several `*.md` files at repo root (`AI_*`, `*_PLAN.md`, `TECH_STACK_*`, etc.). If you do not need them in git, move to a private wiki or delete. If you want them grouped, create `internal-docs/` and move them there (one commit).
2. **Unused images in `optimized/`** — Many PNGs are not referenced from `index.html` (site uses mostly SVGs + `linkedin-banner.svg`). Auditing with a small script or IDE “find references” can shrink the repo; test the site after removing any file.
3. **Typo duplicate** — `ai_consut.png` vs `ai_consulting.png`: confirm which is used before deleting one.

## NAS deploy scripts (`deploy.sh`, `ensure-server-running.sh`)

- **Passwords must not be committed.** These scripts now expect **`NAS_SUDO_PASS`** in the environment (or passwordless `sudo` for `systemctl` on the NAS user).
- If a password was ever committed, **rotate it on the NAS** and avoid re-adding it to the repo.

## Public site URL

See **`WEBSITE_URL.md`** and **`README.md`**.
